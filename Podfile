# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'swift(NSOperation)' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Alamofire'
pod 'HandyJSON'
  # Pods for swift(NSOperation)

  target 'swift(NSOperation)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swift(NSOperation)UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
